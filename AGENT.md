# AGENT.md

## 论文第二版改写工作约定

本文件用于约束后续对毕业论文第二版的分析、改写、补充与输出，避免论文内容与项目实现、数据来源、用户要求不一致。

## 1. 总体原则

1. 论文第二版以当前工作目录 `D:\graduation_thesis` 中的真实项目材料为基础进行改写。
2. 论文中的 `V-KG RAG` 与项目目录 `D:\graduation_thesis\LightRAG` 在本任务中视为等价概念。
3. 若需要描述技术细节，可将 `LightRAG` 的技术原理映射表述为论文中的 `V-KG RAG`，但不得伪造不存在的实验结论或工程实现。
4. 允许参考 [优秀模板2](D:\graduation_thesis\thesis\参考文档\优秀模板2.md) 的结构风格，对现有论文目录、章节层级和标题进行重构。

## 2. 结构改写原则

1. 论文第二版应尽量靠近“优秀模板2”的行文方式，即形成：
   - 绪论
   - 相关技术与开发环境介绍
   - 核心方法设计与实现
   - 系统设计/实现/实验分析
   - 总结与展望
2. 允许新增缺失的小节，例如：
   - 研究目标
   - 研究方法与技术路线
   - 本体设计
   - 数据集构建
   - 实验设置
   - 误差分析
3. 改写目标不是简单润色，而是把初稿补全成更完整、更像正式毕业论文的第二版。

## 3. 信息来源优先级

后续写作时，信息来源按以下优先级使用：

1. 用户明确说明的事实与约束
2. 工作区内真实项目代码与文档
3. `D:\graduation_thesis\LightRAG` 项目内容
4. `D:\graduation_thesis\docs` 中现有图片与图示
5. `D:\graduation_thesis\output` 中的评测输出
6. 必要时联网搜索 `LightRAG` 官方/原始技术信息

## 4. 数据使用规则

1. 实验与统计结论以 `D:\graduation_thesis\output\fixed_evaluation_progress.jsonl` 为最终准据。
2. 若 `output` 下其他统计文件、CSV、图表与 `fixed_evaluation_progress.jsonl` 不一致，应以 `fixed_evaluation_progress.jsonl` 为准重新组织表述。
3. 论文中不得体现“测评框架中存在分数修正逻辑”这一事实。
4. 若后续需要生成新的统计表、汇总表或结论，应显式基于 `fixed_evaluation_progress.jsonl` 推导。
5. 不得为本次论文改写任务重新调用、重跑或触发测评框架接口，包括但不限于 `run`、`evaluate`、`fix`、`plot`、`pipeline` 等流程。
6. 本次写作一律以现有项目产物、第一版论文中已经使用的数据，以及 `output` 目录下的既有结果为准，不进行新的耗时测评。

## 5. 图表与材料规则

1. 论文所需图片优先从 `D:\graduation_thesis\docs` 中寻找。
2. 若项目中存在可用截图、流程图、结果图，应优先复用，而非凭空描述。
3. 若某张图、某个表、某项统计、某个接口截图在项目中找不到，允许先留占位符。
4. 所有占位内容必须在最终另行输出的“缺失项统计文件”中列出，明确告诉用户后续需要手动补什么。

## 6. 可补写与不可补写的边界

### 允许补写

1. 基于 LightRAG/V-KG RAG 原理的技术细节展开
2. 结合现有实现与论文风格进行方法论补充
3. 衔接性段落、学术化表达、章节总结
4. 对现有实验结果的更规范化学术表述
5. 在不违背项目事实前提下补充流程说明、算法步骤说明、模块说明

### 不允许补写

1. 编造不存在的系统模块
2. 编造不存在的数据库、中间件或部署架构
3. 编造不存在的实验结果、性能数字、样本规模、消融实验结论
4. 编造用户未提供、项目中也无法找到的截图或图表内容
5. 在论文中写入“分数修正”“人工修分”“后处理改分”等内容

## 7. 占位符规则

1. 对于无法从项目中找到但论文需要的内容，可以留明确占位符，例如：
   - `[待补图：系统总体架构图]`
   - `[待补表：本体Schema设计表]`
   - `[待补数据：系统响应时间统计]`
2. 占位符必须具体，不能写成模糊的“待补内容”。
3. 第二版论文完成后，必须额外输出一份缺失项统计文件，逐条列出需要用户后续人工补充的材料。

## 8. 文件输出规则

1. 第二版论文必须新建文件，不覆盖原稿。
2. 输出文件默认命名为：
   - `D:\graduation_thesis\thesis\毕业论文第二版：果园病虫害智能问答系统.md`
3. 缺失项统计文件应与第二版论文放在同目录下，单独成文。

## 9. 技术探索规则

1. 如需补充 V-KG RAG/LightRAG 技术细节，可先探索：
   - `D:\graduation_thesis\LightRAG`
   - `D:\graduation_thesis\thesis\参考文档\lighRAG.txt`
   - `D:\graduation_thesis\thesis\参考文档\lightrag_key_content.txt`
2. 若本地资料不足，可联网检索 LightRAG 的原始技术资料。
3. 使用联网资料时，应优先吸收技术思想，不要大段照搬原文。

## 10. 当前任务状态

当前仅完成规则落档。

下一步任务应为：

1. 基于本文件生成论文第二版优化计划
2. 按优化计划重写第二版论文
3. 生成缺失项统计文件
