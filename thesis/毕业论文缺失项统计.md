# 毕业论文缺失项统计

本文件为论文全局维护清单，不再绑定某一版次。以后每次更新论文正文，都应同步更新本文件，删除已经补齐的项，新增本轮改动引入的待确认项。

当前对应稿件：

- `thesis/毕业论文第三版：果园病虫害智能问答系统.md`

## 一、建议后续补成正式表格的数据项

### 1. 本体 Schema 或实体关系设计表

建议补充内容：

- 实体类型列表
- 关系类型列表
- 关键字段说明
- 每类实体/关系的典型示例

当前状态：

- 第三版正文已较详细说明实体、关系、描述信息和去重思路，但仍是文字化表述，没有形成正式表格。

### 2. 知识库规模统计表

建议补充内容：

- 原始知识文档数量
- 总字数或总 token 数
- 切分后的 chunk 数量
- 抽取实体数、关系数、描述数
- 若能拿到，则补充图节点数、边数

当前状态：

- 项目中可确认知识文档和 LightRAG 存储文件存在，但论文可直接引用的完整统计表尚未整理。

### 3. 检索模式或参数配置表

建议补充内容：

- `local / global / hybrid / mix` 等模式的含义
- 当前论文实验实际使用的模式
- `top_k`、`chunk_top_k` 等关键参数

当前状态：

- 第三版正文已经写入当前工程默认使用 `mix` 模式，但还没有整理成正式参数表。

## 二、建议补充的系统材料

### 4. 系统界面或运行截图

建议补充内容：

- 命令行运行截图
- 问答结果示例截图
- LightRAG Web UI 或图谱可视化截图

当前状态：

- 第四章仍以原型结构和实验流程描述为主，没有插入运行界面材料。

建议来源：

- `LightRAG` 自带 Web UI
- 本地 CLI 运行截图
- 若已有答辩展示图，也可复用

### 5. 更细粒度的模块交互图

建议补充内容：

- `问题输入 -> 查询分层 -> 图/向量检索 -> 上下文拼装 -> 生成 -> 评测`

当前状态：

- 现有总体架构图可以支撑论文主线，但如果你想强化“系统设计感”，建议再补一张模块调用链图。

## 三、建议补充的实验项

### 6. 响应时间、检索耗时或更新耗时统计

建议补充内容：

- Pure LLM 平均响应时间
- Naive RAG 平均响应时间
- V-KG RAG 平均响应时间
- 若能测到，则补充检索耗时、生成耗时、索引更新时间

当前状态：

- 第三版已在方法和展望中强化了“轻量检索、增量更新”的论述，但正文仍没有实验型耗时数据支撑。

### 7. 消融实验

建议补充内容：

- 去掉图增强索引后的结果
- 仅局部检索或仅全局检索的结果
- 去掉 `mix` 融合后的结果
- 去掉引用约束后的结果

当前状态：

- 第三版已经把双层检索、邻域扩展、混合检索写得更完整，因此消融实验的重要性比前一版更高，但当前项目没有现成结果可直接引用。

## 四、需要你后续人工确认的事实项

### 8. 知识来源范围是否需要扩充

当前写法：

- 以 `经济果林病虫害防治手册` 为核心知识底座来源。

需要确认：

- 是否还应明确加入其他教材、官方防控方案、农技站资料或区域病虫情报。
- 如果第一版或答辩材料里曾使用过其他来源，但当前工作区没有保留，需要后续人工补回。

### 9. 第三版中关于 LightRAG 机制的表述强度

当前写法：

- 第三版已进一步吸收适合论文的方法阐释内容，包括 RAG 的形式化定义、双层检索、关键词分层、图索引去重、邻域扩展、增量更新等。
- 第三版已完成一轮“论文化”改写，整体语气较前一轮更克制，但仍可继续细化个别段落的句式和章节衔接。
- 第三版已完成第二轮精修，方法章节、实验章节和总结章节的口吻已进一步收紧。

需要确认：

- 如果你希望论文严格区分“当前工程已验证”与“参考架构可支持”，可以在第三章或第四章再加一句限定说明。
- 如果后续准备答辩，建议你统一口径：实验已验证的是整体问答效果，形式化公式、关键词分层和增量更新目前主要属于方法设计层面的论证强化。

### 10. 参考文献格式是否要统一为学校模板

当前状态：

- 第三版已补入 LightRAG 文献，但参考文献整体还未按最终学校模板逐条统一排版。
- 第三版已将新增公式改为 Markdown 数学格式，可用于当前 Markdown 渲染，但最终若导出 Word，仍可能需要按学校模板统一编号和版式。

需要确认：

- 是否需要后续统一为 GB/T 7714 或学校指定格式。

## 五、当前已直接复用或引用的图表材料

- `docs/整体架构图.png`
- `docs/Traditional RAG vs. V-KG RAG Workflow.png`
- `docs/Two types of problems.png`
- `docs/Three dimensions.png`
- `docs/LLM-as-a-Judge Evaluation Process.png`
- `output/charts/chart_faithfulness.png`
- `output/charts/chart_comprehensiveness.png`
- `output/charts/chart_relevance.png`
- `output/charts/chart_heatmap.png`
- `docs/Indexing Flowchart - Img Caption.jpg`
- `docs/Retrieval and Querying Flowchart - Img Caption.jpg`

## 六、当前优先级最高的三项补充

1. 知识库规模与实体关系统计表
2. 系统运行或图谱界面截图
3. 检索模式与耗时/消融实验数据
